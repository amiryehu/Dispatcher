import styled from "styled-components";
import { colors } from "../../Utils/Constances";

export const Aavatar = styled.div`
    background-color: ${colors.blue};
    border-radius: 50%;
    height: 80%;
`;